{
  "name": "nimbus",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "css-build": "node-sass --output-style compressed --source-map true --source-map-contents true --precision 6 style/main.scss style/main.min.css",
    "css-watch": "npm run css-build -- --watch",
    "start": "npm run css-watch",
    "jsminify": "terser js/main.js --compress --mangle --ecma 2016 --output js/main.min.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.14.1"
  },
  "dependencies": {
    "core-js": "^3.6.5",
    "terser": "^4.8.0"
  }
}
